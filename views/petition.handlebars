<div class="petition-container">
    <div class="title-container">
        <h1>{{title}}</h1>
    </div>
    <div class="petition">
        <p>Sign our petition to make whatever this petition supports actually happen!</h3>
        <div class="petition-form">
            <p>This is where an error message should go?</p>
            <form method="POST">
                <input type="text" name="first" placeholder="First Name" autocomplete="off">
                <input type="text" name="last" placeholder="Last Name" autocomplete="off">
                <canvas id="canvas" width="600" height="200"></canvas>
                <input type="hidden">
                <button type="submit">Submit</button>
            </form>
        </div>
    </div>
    <p class="proj-footer">© Rosaria Gandar 2020</h3>
</div>